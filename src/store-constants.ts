/**
 * @constant UPDATE_JWT
 * @description Provide updating all JWT data (access token and expiration time) to the store.
 */
export const UPDATE_JWT = 'UPDATE_JWT'

/**
 * @constant UPDATE_CURRENT_USER
 * @description Provide updating a user data to the store.
 */
export const UPDATE_CURRENT_USER = 'UPDATE_CURRENT_USER'

/**
 * @constant UPDATE_CURRENT_PROJECT
 * @description Provide updating a project data to the store.
 */
export const UPDATE_CURRENT_PROJECT = 'UPDATE_CURRENT_PROJECT'

/**
 * @constant REMOVE_CURRENT_STATE
 * @description Provide removing all states from the store.
 */
export const REMOVE_CURRENT_STATE = 'REMOVE_CURRENT_STATE'
